# 0010) Architecture

AI Release Radar nasce come repository dedicato e provider-ready.

Pipeline V1:

Fetch
→ Parse/Normalize
→ Snapshot
→ Diff
→ Classify
→ Impact
→ Aggregate/Dedup
→ Report
→ Recommend opzionale

La V1 parte con OpenAI/Codex, ma l'architettura deve permettere provider futuri.
